using UnityEngine;

public class PointOffsetHandler : MonoBehaviour
{
    InputHandler input;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        input = InputHandler.Instance;
    }

    // Update is called once per frame
    void Update()
    {
      
    }
}
